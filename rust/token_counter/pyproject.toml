[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "contextual-token-packer"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "contextual_orchestrator._token_packer"
features = ["pyo3/extension-module"]
