# Tracks

| Track | Status | Purpose |
|---|---|---|
| 001-paper-grounded-orchestrator | active | Implement the source-backed orchestration contract with TDD, DDD, and CDD |
| 002-enterprise-design-foundation | active | Add paper-grounded screen design, user stories, REST API, code/DB conventions, and i18n |
| 003-verify-mode-honesty | active | Fail-close `mode=verify` and rejected `conduct`, word-bound verdict terms, stop leftover auto-verify bills (`validate`/`judge`/`확인`/`평가`), echo `reasoning_effort` on `run()`, redact stream verification, invoice every step (including provider `usage`). Per-role profiles stay issue #568. |
