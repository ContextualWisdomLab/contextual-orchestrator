Virtual model routes now retain bounded, secret-free attempt evidence and
exhaust retryable provider transport failures across the admitted candidate
pool before returning the final typed error. Experiential Labs discovery accepts
both credential spellings, and Bytez discovery keeps its raw-token,
task-filtered, bounded-retry contract while tolerating transient catalog
failures and supported page envelopes.
