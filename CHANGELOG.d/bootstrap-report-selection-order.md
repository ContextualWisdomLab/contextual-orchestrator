### Fixed

- Durable provider bootstrap reports now preserve the selector's cost and model-group ordering after persisted identity resolution instead of alphabetically sorting enabled agent IDs. Pool membership and operator-identity collision checks remain fail closed.
