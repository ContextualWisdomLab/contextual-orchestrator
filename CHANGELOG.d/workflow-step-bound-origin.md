The generated-plan step bound (`OrchestrationPolicy.max_workflow_steps`, default 6) now records its origin next to the field and in `docs/architecture.md`: a product decision (template needs four steps; six leaves one extra worker plus one repair/verify step), explicitly not the Fugu-Ultra report's "up to 5 steps" training setting (arXiv:2606.21228 S3.2.3) and separate from the effort catalog's per-profile bound. `tests/test_paper_contracts.py::test_generated_plan_bound_comes_from_policy` locks that the planner prompt and the plan parser read the same policy value.
