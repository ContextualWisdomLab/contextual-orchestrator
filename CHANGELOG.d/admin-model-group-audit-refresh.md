Fixed the admin console's Audit tab going stale after saving or deleting a model group — saving/deleting only re-fetched the model group list, not `/admin/state`, which is the only source for `recent_audit_events`. Both handlers now also refresh the audit events via a lightweight helper that re-fetches `/admin/state` without re-triggering the heavier full `load()` path (readiness/commercial endpoints, `simulate()`). The model-group feedback text also now reflects success/error with color, matching the rest of the console's status conventions.
