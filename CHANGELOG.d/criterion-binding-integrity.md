### Fixed

- Recompute the immutable criterion-set digest over scope, rules, categories,
  and order; seal criterion value construction; replay integrity checks; and
  require provider observations to match a separately trusted criterion set.
