Added an `x-request-id` response header carrying the server-generated request identity on every gateway response (JSON, buffered SSE, and incremental SSE), the same value error payloads already expose as `error.detail.request_id`. A caller can now correlate a *served* request with gateway logs and provider evidence instead of only failed ones (#1016). The value is never taken from a caller-supplied header.
