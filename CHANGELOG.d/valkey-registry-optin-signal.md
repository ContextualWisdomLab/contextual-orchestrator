`build_job_registry` now logs a warning when `batch_job_registry_valkey_url` is configured but the `redis` client package (the `queue` extra) is missing. That deployment opted into durable job registries and previously received in-process dicts with no signal, indistinguishable from never having configured the URL. The degradation itself is unchanged: startup still succeeds with in-process registries rather than failing a serving gateway.
