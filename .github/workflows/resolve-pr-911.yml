name: Resolve PR 911 against protected main

# Diagnostic retry: report bounded diff3 hunks for executable conflicts.
on:
  push:
    branches: [feat/durable-routing-observations-20260829]

permissions:
  contents: write
  pull-requests: write

jobs:
  resolve:
    uses: ContextualWisdomLab/contextual-orchestrator/.github/workflows/reusable-pr-conflict-resolver.yml@automation/one-shot-pr-conflict-resolver
    with:
      pr_number: 911
      branch: feat/durable-routing-observations-20260829
      caller_file: .github/workflows/resolve-pr-911.yml
