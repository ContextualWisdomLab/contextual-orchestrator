name: Resolve PR 914 against protected main

on:
  push:
    branches: [fix/chat-stream-usage-compat]

permissions:
  contents: write
  pull-requests: write

jobs:
  resolve:
    uses: ContextualWisdomLab/contextual-orchestrator/.github/workflows/reusable-pr-conflict-resolver.yml@automation/one-shot-pr-conflict-resolver
    with:
      pr_number: 914
      branch: fix/chat-stream-usage-compat
      caller_file: .github/workflows/resolve-pr-914.yml
