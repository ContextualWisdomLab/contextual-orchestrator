name: Resolve PR 908 against protected main

on:
  push:
    branches: [feat/metering-export-pr-20260828]

permissions:
  contents: write
  pull-requests: write

jobs:
  resolve:
    uses: ContextualWisdomLab/contextual-orchestrator/.github/workflows/reusable-pr-conflict-resolver.yml@automation/one-shot-pr-conflict-resolver
    with:
      pr_number: 908
      branch: feat/metering-export-pr-20260828
      caller_file: .github/workflows/resolve-pr-908.yml
