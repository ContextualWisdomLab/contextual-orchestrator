name: Resolve PR 913 against protected main

# Hardened diagnostic retry after resolver correction.
on:
  push:
    branches: [feat/responses-stream-usage-20260829]

permissions:
  contents: write
  pull-requests: write

jobs:
  resolve:
    uses: ContextualWisdomLab/contextual-orchestrator/.github/workflows/reusable-pr-conflict-resolver.yml@automation/one-shot-pr-conflict-resolver
    with:
      pr_number: 913
      branch: feat/responses-stream-usage-20260829
      caller_file: .github/workflows/resolve-pr-913.yml
